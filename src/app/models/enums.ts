export enum Language {
  ENGLISH = 'en_EN',
  SPANISH = 'es_ES',
}

export enum TypeLanguage {
  CSS = 'language-css',
  HTML = 'language-html',
}

export enum FlexDirection {
  ROW = 'row',
  ROWREVERSE = 'row-reverse',
  COLUMN = 'column',
  COLUMNREVERSE = 'column-reverse',
}

export enum FlexWrap {
  WRAP = 'wrap',
  NOWRAP = 'nowrap',
  WRAPREVERSE = 'wrap-reverse',
}

export enum JustifyContent {
  FLEXSTART = 'flex-start',
  FLEXEND = 'flex-end',
  CENTER = 'center',
  SPACEBETWEEN = 'space-between',
  SPACEAROUND = 'space-around',
  SPACEEVENLY = 'space-evenly',
}

export enum AlignItems {
  STRETCH = 'stretch',
  FLEXSTART = 'flex-start',
  CENTER = 'center',
  FLEXEND = 'flex-end',
  BASELINE = 'baseline',
}

export enum Grid {
  GRID = 'grid',
  INLINEGRID = 'inline-grid',
}

export enum Implicit {
  ROW = 'rows',
  COLUMN = 'columns',
}

export enum Explicit {
  ROW = 'example row',
  COLUMN = 'example column',
}

export enum AutoFlow {
  ROW = 'row',
  COLUMN = 'column',
  DENSE = 'dense',
}

export enum Positioning {
  COLUMNROW = 'grid-column / grid-row',
  AREA = 'grid-area',
}

export enum JustifyGrid {
  START = 'start',
  END = 'end',
  CENTER = 'center',
  STRETCH = 'stretch',
}
export enum AlignGrid {
  START = 'start',
  END = 'end',
  CENTER = 'center',
  STRETCH = 'stretch',
}

export enum ContentGrid {
  START = 'start',
  END = 'end',
  CENTER = 'center',
  STRETCH = 'stretch',
  SPACEBETWEEN = 'space-between',
  SPACEAROUND = 'space-around',
  SPACEEVENLY = 'space-evenly',
}
